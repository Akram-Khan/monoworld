.row.comment-show{:class => cycle("dark", "light") }
	-if @photo.comments.exists?
		-@photo.comments.each do |comment|
			.row
				=comment.comment
				//=comment.user.firstname
				//=comment.user_id
-reset_cycle
.container